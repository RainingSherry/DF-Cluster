# 通用表格零样本 Clean-Geometry Diffusion 聚类实验方案（Generator V4 执行稿）

> 本文档按照“做什么、怎么做”的顺序描述当前主动方案。明确不执行的内容、历史结论的处理方式和尚未冻结的扩展统一放在文末。

## 一、研究目标

训练一个面向通用表格的 zero-shot clustering 模型。模型首先把不同样本数、不同特征数、混合数据类型和不同缺失模式的观测表格编码成统一的 128 维 observation representation，再使用条件扩散桥恢复旋转不变的 clean clustering geometry，最后使用固定 known-K KMeans 得到聚类结果。

主实验回答三个问题：

1. 复杂、可恢复的 `Z_clean → X` observation process 能否构成稳定的合成预训练分布。
2. 中等偏大的 Global AE 能否把异构表格整理成统一的 128 维 observation representation。
3. DDBM 能否把 observation representation 恢复为更接近 clean Gram、clean distances 和 pairwise co-assignment 的聚类几何，并提高恢复表示上的 CLM 与冻结 known-K KMeans 聚类质量。

## 二、符号和数据对象

- `N`：一个 task 的样本数，主训练范围为 128–2048。
- `D`：一个 task 的观测特征数，主训练范围为 10–160。
- `K`：簇数量，范围为 2–20。
- `d_int`：clean latent 的有效维数，范围为 2–128。
- `Y`：合成 task 的真实簇标签，形状为 `N`。
- `Z_signal`：在 `d_int` 维有效空间中生成的 clean cluster signal，形状为 `N×d_int`。
- `Q`：把有效空间嵌入 128 维 ambient space 的正交基，形状为 `128×d_int`。
- `Z_clean`：128 维 clean clustering latent，`Z_clean = Z_signal Qᵀ`，形状为 `N×128`。
- `R_nuisance`：与观测过程有关的连续、离散、环境和噪声根变量。
- `X`：由 `Z_clean` 与 `R_nuisance` 经过 observation graph 生成的表格，形状为 `N×D`。
- `T_feature`：每列的 numerical、categorical、ordinal、count、bounded 类型标记。
- `M_missing`：与 X 同形状的 missing mask。
- `G_clean`：中心化并归一化后的 sample Gram，形状为 `N×N`。
- `D_clean`：由 `G_clean` 导出的成对平方距离。
- `A_pair`：由 Y 构造的 pairwise co-assignment target，`A_pair,ij=1[Y_i=Y_j]`。
- `CLM_observed`：在 `(X,Y)` 上使用冻结官方 `CH_A` 实现计算的 Cluster-Label Matching 标量。
- `CLM_clean`：在 `(Z_clean,Y)` 上使用同一 `CH_A` 实现计算的 clean-control CLM 标量。
- `CLM_recovered`：在 `(B_hat,Y)` 上使用同一 `CH_A` 实现计算的恢复后 CLM 标量。
- `E_phi`：Global AE encoder。
- `D_phi`：Global AE decoder。
- `H_obs`：AE 输出的 observation representation，形状为 `N×128`。
- `B_theta`：dataset-context DDBM。
- `B_hat`：DDBM 输出的 128 维 clean factor，形状为 `N×128`。
- `G_hat`：预测几何，`G_hat=B_hat B_hatᵀ`。

## 三、完整执行流程

```text
task specification
    → Y 与 KMeans-friendly Z_clean
    → clean Gram / distances / pairwise co-assignment / CLM audit
    → nuisance roots
    → TabICL-adapted SCM + MITRA-inspired tree nodes + label-free flow
    → mixed-type feature heads、noise、missingness、redundancy
    → X-only input shard 与 target shard
    → Global AE: X → H_obs
    → dataset-context DDBM: H_obs → B_hat
    → G_hat=B_hat B_hatᵀ
    → frozen known-K KMeans(B_hat)
    → synthetic paired evaluation / real zero-shot evaluation
```

## 四、代码与来源准备

1. 固定 `baseline/external/tabicl_icml2025` 作为 TabICL 论文期 MLP-SCM/Tree-SCM 的可核查来源。
2. 固定 `baseline/external/tabicl` 作为当前 GraphSCM 和扩展节点函数的可核查来源。
3. 固定 `baseline/external/tabclustpfn` 作为 controlled-overlap GMM、直接 partition baseline 和 ZEUS-style prior 的可核查来源。
4. 固定 `baseline/external/clm` commit `927927f8e3cd8874fef056d06defbee215a55173` 的 adjusted Calinski-Harabasz `CH_A` 作为唯一 CLM 实现，固定 logistic `k=4.432010535838295`。
5. 新建 `methods/dfcluster/generator_v4/`，将本项目 observation generator 与外部源码隔离。
6. 在 `generator_v4/provenance.yaml` 中逐项记录上游论文、commit、许可证、复用文件、本项目修改和本项目新增参数。
7. 将 MITRA 部分实现命名为 `mitra_inspired_tbp`。为 DT、ET、RF、GB、direct-RF 分别建立模块，并在每个模块顶部记录对应论文段落和本项目自行决定的参数。
8. 为所有 generator component 计算 source SHA，并把 SHA 写入每个 task manifest。

## 五、采样 task 规模和 schema

1. 使用 task seed 生成唯一 `task_id`，格式为 `generator_version/source_split/seed/task_index`。
2. 在主训练范围内按 log-uniform 采样 N 和 D：`N∈[128,2048]`，`D∈[10,160]`。
3. 在整数集合 `{2,…,20}` 中采样 K，并保证每个簇至少包含 `max(8, ceil(0.01N))` 个样本。
4. 在整数集合 `{2,…,128}` 中按 `P(d_int)∝1/sqrt(d_int)` 采样有效维数，使所有维数均有非零概率且低维 task 数量更多。
5. 从 `{0,0.10,0.25,0.50}` 中采样 categorical/ordinal/count 列的目标比例，其余列作为 numerical 或 bounded numerical。
6. 从 `{0,0.05,0.10,0.20,0.30}` 中采样 task-level missing-rate 档位，再由具体 missing mechanism 分配到单元格。
7. 对生成后的列执行随机 permutation，并把 permutation 写入 metadata。

## 六、生成 Y 与 clean clustering geometry

1. 从 log-uniform `alpha∈[0.3,10]` 采样 Dirichlet concentration，生成不均衡簇权重。
2. 根据簇权重采样 Y，并执行最小簇大小校验；未通过的 task 使用同一 task id 派生的 deterministic retry seed 重新生成。
3. 在四种 clean family 中等量覆盖：
   - controlled-overlap elliptical Gaussian mixture；
   - elliptical Student-t / heavy-tailed mixture；
   - low-rank factor/subspace mixture；
   - hierarchical prototype mixture。
4. 为每个簇采样中心、尺度、协方差结构、偏心率、尾部参数和簇内子模态参数。
5. 使用 controlled overlap 参数控制簇间 Mahalanobis separation，并记录 pairwise overlap summary。
6. 生成 `Z_signal∈R^(N×d_int)`，对每个 task 做全局中心化和统一 RMS scale normalization。
7. 采样 `Q∈R^(128×d_int)` 的正交列基，计算 `Z_clean=Z_signal Qᵀ`。
8. 计算 `ARI(KMeans(Z_clean,K),Y)`；将达到 clean-oracle gate 的 task 写入主候选池。

## 七、生成旋转不变的 clean targets

1. 计算中心化矩阵 `H=I−11ᵀ/N` 和 `Z_c=HZ_clean`。
2. 计算 `G_clean=Z_c Z_cᵀ`。
3. 使用 `trace(G_clean)/N` 对 Gram 进行尺度归一化。
4. 由 Gram 计算 `D_clean,ij²=G_ii+G_jj−2G_ij`。
5. 根据 Y 定义 `A_pair,ij=1[Y_i=Y_j]`，qualification/audit task 保存 bit-packed upper triangle。
6. 对 `(X,Y)` 与 `(Z_clean,Y)` 分别运行冻结的官方 `CH_A`，得到 `CLM_observed` 与 `CLM_clean`。
7. CLM 输入使用固定预处理：mask-aware column median imputation、categorical numeric-code representation、逐列 mean/std standardization、constant column 映射为 0。
8. qualification/audit task 保存两个 CLM 标量、bit-packed `A_pair` 和必要的 Gram/distance 压缩表示。
9. online training task 保存 Y、Z_clean 和 seed，在训练进程中按需计算 Gram、sampled distances 与 sampled co-assignment pairs；CLM 标量写入独立 audit sidecar。

## 八、生成 nuisance roots

1. 为每个 task 生成独立连续 nuisance roots，覆盖 Gaussian、Student-t、Laplace 和 bounded distributions。
2. 生成离散 environment roots、batch roots 和随机尺度 roots。
3. 采样与 Z 的部分维度存在连续函数关系的 nuisance descendants，使观测中同时存在相关干扰和独立干扰。
4. 将 `U=[Z_clean,R_nuisance]` 作为 observation graph 的唯一根输入。
5. 为每个根节点保存来源、分布、维度和 seed。

## 九、构造统一 mixed observation graph

1. 为每个 task 采样一张 DAG，根节点为 U，叶节点连接到 D 个 observation feature heads。
2. 将从 root 到 informative leaf 的路径深度采样为 3–8。
3. 将每个中间节点的父节点数量采样为 1–5，并限制图中全部节点都可由根到达。
4. 每个主训练 task 至少使用两类不同的节点函数。
5. 节点函数池由四个可审计 strata 组成：
   - TabICL-adapted Graph/MLP-SCM nodes；
   - TabICL-adapted Tree-SCM nodes；
   - MITRA-inspired DT/ET/RF/GB/direct-RF nodes；
   - label-free invertible/analytic nodes。
6. qualification corpus 对四个 strata 等量生成，并额外生成 mixed-node DAG；正式 online generator 按冻结 manifest 维持同样的 family coverage。
7. 每个 task 至少生成一个 nuisance-only branch 和一个多父节点 interaction branch。
8. 对中间节点输出执行 robust scaling、finite check 和异常幅度裁剪，并记录裁剪比例。

## 十、接入 TabICL 生成机制

1. 从 TabICL 论文期 MLP-SCM/Tree-SCM 与当前 GraphSCM 中抽取节点函数、随机图构造和函数参数采样逻辑。
2. 把原有内部 root sampler 替换为 `Z_clean` 与 `R_nuisance` root injection。
3. 将原有 supervised `(X,y)` 返回接口改为 `generate_observation(U,spec,seed)→X_continuous,graph_metadata`。
4. 对每个 descendant 保存父节点、函数类型、参数、噪声和执行顺序。
5. 为 root injection、同 seed 重放、行 permutation equivariance、有限值和 shape 建立测试。

## 十一、接入 MITRA-inspired tree prior

1. 只实现 MITRA 论文明确列出的 tree family：DT、ET、RF、GB 和 direct-RF。
2. 使用公开标准 tree/ensemble 算法作为执行内核，把父节点值作为输入，把随机生成的连续或离散 descendant value 作为拟合目标。
3. 为每个 tree node 保存算法类型、深度、叶节点限制、feature subsampling、ensemble size、随机目标生成方式和 seed。
4. 将论文没有公开的参数统一放入 `dfcluster_design_choices` 配置域。
5. 在结果和代码中使用 `MITRA-inspired TBP` 名称，并在 provenance 中区分“论文明确描述”与“本项目实现”。
6. 为五个 tree family 分别建立 deterministic replay、非恒等输出、非有限值、退化单叶和信息完全坍缩测试。

## 十二、接入 label-free flow 与 analytic nodes

1. 实现不接收 Y 的 iResNet/flow block，输入仅为当前 parent variables。
2. 添加 linear、low-rank projection、spline、monotone、periodic、product、max/min、RFF 和 saturation 节点。
3. information-preserving stratum 使用维度保持的 flow、单调变换和冗余展开。
4. noisy-but-recoverable stratum 使用异方差噪声、随机尺度、outlier、相关 nuisance 和冗余列。
5. controlled-lossy stratum使用低秩投影、quantization、threshold、随机遮蔽和有限分辨率离散化。
6. 三个 information strata 在 qualification corpus 中等量覆盖。

## 十三、生成 mixed-type X

1. numerical head 输出连续值，并使用 robust task-level normalization 保留列间尺度差异 metadata。
2. bounded head 使用 sigmoid/tanh 后映射到随机区间。
3. ordinal head 使用随机有序阈值将连续 descendant 分箱。
4. categorical head 使用 quantile bins、随机类别合并或多 logit argmax 生成类别。
5. count head 使用非负 rate 生成 Poisson/negative-binomial 风格计数。
6. 添加 informative、redundant、correlated nuisance 和 irrelevant feature，并记录每类列索引。
7. 应用 MCAR、MAR 和基于遮蔽前 feature value 的 MNAR mechanism，输出 `M_missing`。
8. 统一输出 `X`、`T_feature`、`M_missing` 和 column-level generation metadata。

## 十四、生成 complexity certificate

每个 task 写出以下字段：

1. DAG 节点数、边数、深度、分支数和父节点统计。
2. 从 Z root 到每个 X leaf 的路径长度。
3. 使用的函数 family、函数数量和参数摘要。
4. invertible、stochastic、many-to-one 节点数量。
5. informative、redundant、nuisance、irrelevant 列比例。
6. numerical/categorical/ordinal/count/bounded 列比例。
7. missingness、noise、outlier 和 clipping 比例。
8. X 的有效秩、条件数、重复行/列数量和 non-finite count。
9. clean/raw 距离相关性和 kNN neighborhood preservation。
10. clean-oracle ARI、raw-X ARI、ARI headroom、`CLM_observed`、`CLM_clean`、CLM headroom 与 supervised recoverability。
11. generator version、source SHA、config SHA、task seed 和 input/target artifact SHA。

## 十五、执行 generator validity gate

1. 对 X 使用统一冻结的 mixed-type preprocessing，生成 raw clustering control。
2. 使用 known-K KMeans 计算 `ARI_raw`。
3. 计算 `ARI_clean` 和 `headroom=ARI_clean−ARI_raw`。
4. 使用冻结官方 `CH_A` 计算 `CLM_observed`、`CLM_clean` 和 `CLM_headroom=CLM_clean−CLM_observed`。
5. 使用冻结的 ExtraTrees probe 做 5-fold stratified cross-validation，参数固定为 `n_estimators=256`、`max_features=sqrt`、`class_weight=balanced`、`random_state=20260824`，记录 macro-F1。
6. 主候选 task 使用以下 gate：
   - `ARI_clean≥0.90`；
   - `headroom≥0.20`；
   - supervised probe macro-F1 `≥0.75`；
   - X、Z、Gram、distance 全部有限；
   - cluster size、shape、rank 和 metadata 契约通过。
7. 按 raw ARI 建立 easy `[0.50,0.80]`、medium `[0.15,0.50)`、hard-but-recoverable `<0.15` 三个难度池；qualification 阶段对三个难度池等量取样。
8. 在每个 raw-ARI 难度池内再按 `CLM_observed` 经验三分位等量取样，使 CLM 作为独立难度/标签匹配轴进入 corpus coverage。
9. 生成少量 impossible-control tasks，并在 manifest 中单独标记，供 hallucinated recovery 检查使用。
10. validity gate 只读取 synthetic target，不读取真实 benchmark 标签，也不读取 DDBM 结果。

## 十六、构造 paired counterfactual tasks

1. 对同一个 `(Y,Z_clean,R_nuisance)` 分别运行 Graph/MLP-SCM、Tree-SCM、MITRA-inspired TBP、flow 和 mixed DAG。
2. 为各版本使用相同的 N、K、d_int、簇权重和 clean geometry。
3. 为各版本保存共同的 `parent_task_id`。
4. 分别记录 observation family 对 raw ARI、CLM、距离、邻域、秩、missingness 和 recoverability 的影响。
5. synthetic evaluation 按 parent task 做 paired comparison。

## 十七、数据划分和存储

1. 使用 `/data/luolie/DF-Cluster/data/generator_v4/` 作为大体积数据根目录。
2. 建立 `contract/`、`qualification/`、`train_replay/`、`validation/`、`mechanism_ood/` 和 `manifests/`。
3. `input/` shard 只保存 X、feature types、missing mask 和允许公开的 schema metadata。
4. `targets/` shard 保存 Y、Z_clean、Gram/distance replay 信息和 bit-packed `A_pair`；`audit/` sidecar 保存 `CLM_observed`、`CLM_clean`、官方实现 hash 和预处理说明。
5. 使用不同进程/loader 读取 input 与 targets；encoder forward 的调用接口只接收 input shard。
6. 大规模训练采用在线生成，只保存 generator config、seed、task certificate、rejection ledger 和固定 replay shard。
7. validation 固定 seed、完整 task manifest 和 artifact SHA。
8. mechanism-OOD 按整类函数组合或参数尾部留出，不使用普通随机行切分替代机制留出。

## 十八、Global AE 输入编码

1. 数值 cell 使用 value projection、missing indicator 和 column-statistics token 编码。
2. 类别/序数 cell 使用 task-local category embedding 与 feature-type embedding。
3. 对每列使用 distribution-aware Set Transformer 在样本维进行聚合。
4. Column Set Transformer 固定为 4 blocks、hidden dimension 512、8 heads、FFN 2048。
5. 对每行的 column tokens 使用 row/schema Transformer 建模列间关系。
6. Row/schema Transformer 固定为 8 blocks、hidden dimension 512、8 heads、FFN 2048。
7. 使用 8 个 hidden-512 Perceiver latent queries 聚合可变 D 的 row representation。
8. 将 Perceiver 输出投影为每个样本 128 维 `H_obs`。

## 十九、Global AE decoder 与训练目标

1. Decoder 使用 `H_obs` 与 contextual column tokens 逐列产生 reconstruction distribution。
2. numerical/bounded 列使用 standardized Huber reconstruction。
3. categorical/ordinal 列使用 cross-entropy reconstruction。
4. count 列使用 count likelihood 或稳定的 log1p-Huber reconstruction。
5. missing mask 使用独立 binary reconstruction head。
6. 训练输入执行 masked-column、masked-cell、随机噪声、随机 scale 和 feature dropout corruption。
7. AE checkpoint 单独保存 encoder、decoder、optimizer、source/config SHA 和 validation reconstruction metrics。
8. AE 训练完成后冻结 encoder，使用冻结 `E_phi` 为 DDBM 产生 H_obs。

## 二十、DDBM 训练对象与几何输出

1. 每个训练 task 构成配对对象 `(H_obs,Z_clean,G_clean,D_clean,A_pair)`；CLM 标量用于 corpus stratification、audit 和 evaluation。
2. DDBM 的 noisy state 和最终输出形状固定为 `N×128`。
3. DDBM 输出 `B_hat`，并计算 `G_hat=B_hat B_hatᵀ`。
4. 对 `B_hat` 做 task-level centering 和 RMS normalization，再进入 Gram、distance 和 clustering evaluation。
5. 使用全体样本作为 dataset context，并对不同 N 使用 padding mask。
6. 对 N≤2048 的 task 直接计算完整 Gram；训练 loss 可对 off-diagonal pair 做无偏抽样以控制显存。
7. 保存 diffusion trajectory seed、time sample、noise seed 和 task id，使每个训练对可重放。

## 二十一、中等偏大 DDBM 主模型

1. 使用 16 个 conditional Transformer blocks。
2. hidden dimension 固定为 768，attention heads 固定为 12，FFN dimension 固定为 3072。
3. 每层包含 noisy-state self-attention、对冻结 AE context 的 cross-attention、time conditioning 和 FFN。
4. clean factor output dimension 固定为 128。
5. diffusion/bridge training steps 固定为 `T=512`。
6. 使用 AdamW，初始 learning rate 为 `1e-4`，warmup 后使用 cosine decay。
7. 使用 bf16、gradient checkpointing 和按 task/cell budget 的 gradient accumulation。
8. 每个 task 保持整表 forward，row order 在进入模型前随机 permutation，并在输出评价时使用 inverse permutation 对齐。

## 二十二、DDBM loss

1. 使用 bridge/noise prediction loss 训练反向去噪过程。
2. 使用 normalized Gram consistency 比较 `G_hat` 与 `G_clean`。
3. 使用 sampled pairwise distance consistency 比较预测与 clean distances。
4. 使用 neighborhood consistency 保持 clean kNN ordering。
5. 使用 pairwise co-assignment auxiliary loss 监督 sampled positive/negative pairs。
6. co-assignment pair sampling 对同簇和异簇 pair 做数量平衡，并按 task 归一化。
7. 将各辅助项按 task 内有效 pair/neighbor 数归一化，固定总损失为 `L_bridge + 1.0L_Gram + 0.5L_distance + 0.25L_neighborhood + 0.10L_pair`。
8. 每项 loss 单独写入 history，记录均值、分位数、梯度范数和 non-finite count。
9. 模型 checkpoint 选择只依据预注册的 synthetic geometry validation summary。

## 二十三、阶段化训练

1. 阶段 A 训练 Global AE，只执行 mixed-type masked/denoising reconstruction。
2. 阶段 A 完成后冻结 AE 参数和 checkpoint SHA。
3. 阶段 B 使用冻结 AE 在线产生 H_obs，并训练 DDBM 恢复 clean geometry。
4. 阶段 B 的 encoder forward 使用 eval mode；DDBM 使用 train mode。
5. 每个 optimizer update 记录接触的 task 数、总样本数、总 cell 数和 family/difficulty 分布。
6. validation 始终使用冻结 task pool、冻结 noise seeds 和冻结聚类协议。

## 二十四、generator contract 与 qualification

1. contract test 对每个 observation stratum 生成 64 个 task，验证 root injection、Y/input 隔离、shape、finite、replay、metadata、row/column permutation 和 target consistency。
2. qualification 使用 3 个固定 generator seed。
3. 每个 seed、每个 observation stratum 生成 4096 个有效 task；四个基本 strata 与 mixed stratum分别报告。
4. qualification 输出 family 内多样性、family 间可区分性、raw-ARI/CLM difficulty coverage、近重复率和 rejection reason 分布。
5. qualification 使用中等偏大的冻结 AE+DDBM 配置做短程端到端可训练性检查。
6. generator 通过 validity、coverage 和稳定性验收后冻结 config/source SHA，再进入正式训练。

## 二十五、正式在线训练

1. 正式训练持续在线生成独立 task，不循环有限训练文件。
2. online sampler 按 qualification 冻结的 observation-family、information-stratum、raw-ARI difficulty 和 CLM tertile coverage 维持等量 task exposure。
3. 第一阶段任务暴露目标为至少 500 万个有效 task；训练 ledger 按真实 task exposure 而不是只按 optimizer step 计数。
4. 在 50 万、100 万、200 万和 500 万 task exposure 保存非选择性里程碑 checkpoint。
5. 使用固定 validation pool 报告学习曲线；里程碑不改变 generator、loss、模型宽度或训练 seed。
6. 至少运行 3 个训练 seed，并保持相同 task manifest distribution 与不同 task stream seed。
7. 每个正式运行写出原子状态、错误 ledger、GPU ledger、source/config SHA 和 checkpoint SHA。

## 二十六、Synthetic evaluation

1. 对固定 synthetic test task 分别计算 raw-X、H_obs、B_hat 和 Z_clean 的 known-K KMeans。
2. 主聚类指标为 ARI；同时报告 NMI、AMI 和 V-measure。
3. 主几何指标为 normalized Gram error、pairwise distance correlation、kNN preservation 和 oracle-gap recovery。
4. 报告 co-assignment pairwise AUROC/AUPRC 和 calibrated pairwise loss。
5. 在 raw-X、H_obs、B_hat 和 Z_clean 上分别计算冻结官方 `CH_A`，报告 `CLM_recovered−CLM_observed` 和相对 clean-control gap。
6. 按 clean family、observation family、information stratum、raw-ARI difficulty、CLM difficulty、N、D、K、d_int 和 missing rate 分层报告。
7. paired counterfactual task 使用共同 parent id 做逐任务 paired difference。
8. 每个 seed 报告 task-level median、q10、q90 和 paired bootstrap confidence interval。

## 二十七、真实数据 zero-shot known-K evaluation

1. 使用冻结 real manifest 逐个读取 X、feature types 和 missing mask。
2. 使用与 synthetic 完全相同的 preprocessing、Global AE 和 DDBM。
3. 对每张表执行整表 forward，得到 `H_obs`、`B_hat` 和 `G_hat`。
4. 使用 manifest 中冻结的 K 对 raw-X、H_obs 和 B_hat 分别执行同一 KMeans 协议。
5. predictions 和全部 provenance 冻结后，独立 scorer 才读取真实 Y。
6. 按 dataset 与 seed 报告 ARI、NMI、AMI、V-measure 和相对于 raw-X 的 paired improvement。

## 二十八、资源与运行规则

1. 大体积数据、replay shard、checkpoint 和正式输出写入 `/data/luolie/DF-Cluster/`。
2. 源码、配置、文献、manifest 和小型审计文件保留在 `/home/luolie/DF-Cluster/`。
3. 新运行优先装入一张允许使用的 GPU；只在该卡剩余显存 `≤20 GiB` 且新运行实测确实无法容纳时增加 GPU。
4. 每个 task 保持单卡整表 forward，不因显存临时改变样本集合、特征集合或 generator family。
5. qualification 先测峰值显存，再确定同卡并发数；正式运行沿用冻结的并发方案。

## 二十九、核心参数汇总

| 项目 | 主配置 |
|---|---|
| N | log-uniform，128–2048 |
| D | log-uniform，10–160 |
| K | 2–20，known-K evaluation |
| d_int | 2–128，`P(d)∝1/sqrt(d)` |
| clean ambient / AE output / DDBM output | 128 |
| clean family | elliptical Gaussian、Student-t、factor/subspace、hierarchical prototype |
| observation nodes | TabICL Graph/MLP-SCM、TabICL Tree-SCM、MITRA-inspired TBP、label-free flow/analytic |
| information strata | preserving、noisy-recoverable、controlled-lossy |
| clean gate | ARI≥0.90 |
| headroom gate | ≥0.20 |
| recoverability gate | ExtraTrees 5-fold macro-F1≥0.75 |
| AE column encoder | 4 blocks / width 512 / 8 heads / FFN 2048 |
| AE row/schema encoder | 8 blocks / width 512 / 8 heads / FFN 2048 |
| Perceiver | 8 queries / width 512 / output 128 |
| DDBM | 16 blocks / width 768 / 12 heads / FFN 3072 |
| diffusion steps | T=512 |
| optimizer | AdamW / lr 1e-4 / weight decay 0.01 / 10k-step warmup / cosine decay to 1e-6 |
| DDBM loss | `1.0 bridge + 1.0 Gram + 0.5 distance + 0.25 neighborhood + 0.10 pair` |
| contract | 64 tasks per observation stratum |
| qualification | 4096 valid tasks × 3 seeds × each stratum |
| formal exposure | ≥5,000,000 valid online tasks / 3 training seeds |
| primary geometry target | centered normalized sample Gram |
| auxiliary geometry target | sampled distances、neighborhood、pairwise co-assignment |
| CLM | official adjusted `CH_A` scalar；用于 audit、difficulty coverage 与 evaluation |
| primary clustering protocol | frozen known-K KMeans |

## 三十、当前不执行的内容

1. 当前不把 Y、K、CLM scalar、clean Gram、generator family id 或 difficulty 字段输入 observation generator、AE 或 DDBM。
2. 当前不把 MITRA-inspired 模块表述为 MITRA 官方 generator 或 MITRA 源码复现。
3. 当前不根据 DDBM、真实数据 ARI 或历史 benchmark performance 选择 generator task。
4. 当前不把随机旋转作为 observation difficulty；旋转只体现在 clean ambient basis 和几何不变性测试中。
5. 当前不使用 VAE、KL regularization、dataset-specific AE 或真实数据 fine-tuning。
6. 当前不使用 CLM 标量取代 clean Gram；CLM 用于 audit、难度覆盖和评价，pairwise co-assignment 作为辅助训练 target。
7. 默认不物理删除旧数据、旧 checkpoint 和旧日志；它们移出 active manifest 并登记为 legacy generator evidence。**例外：用户于 2026-08-25 明确要求物理移除 V3 及其依赖的薄弱 `dfhybrid_v1` 分支；相关实验、数据、代码、配置、测试、review prompt 和缓存已删除，不得影响 V4 决策。**
8. 当前不启动 generator_v4 数据生成或新 GPU 训练，先完成实现、测试、provenance 和 qualification contract。

## 三十一、主线完成后再考虑的事项

1. 在 DDBM 已证明能够学习 clean geometry 后，补充容量匹配的 deterministic refiner，评价扩散的必要性。
2. 当 N 扩展到明显超过 2048 时，将完整 Gram supervision 切换为 landmark distance representation，并比较 Gram/landmark 的误差和吞吐。
3. 增加 unknown-K head，并把 K estimation 与 geometry recovery 分开报告。
4. 增加非 KMeans-friendly clean geometry，例如 rings、moons、spirals 和一般 manifold，并同步更换适合该几何的下游聚类器。
5. 在冻结 AE+DDBM 主实验完成后，评估小学习率 joint fine-tuning。
6. MITRA 官方 generator 源码公开后，增加官方实现与 `MITRA-inspired TBP` 的逐项差异审计。
7. 在完整 inventory、hash 和汇总归档完成后，再决定是否删除 `/data` 中旧弱 generator 的大体积数据。

## 三十二、历史结论的当前状态

1. V1/V2 结果只保留为对应 legacy generator、对应模型和对应训练协议上的历史观测。
2. AE 是否必要、DDBM 是否有效、Transformer 容量是否足够、训练任务是否过易、样本暴露是否足够和旋转是否造成主要性能损失，全部在 Generator V4 上重新检验。
3. 数学上的几何不可辨识性、标签输入边界、source provenance、真实评测 X/Y 隔离和固定 known-K 协议继续作为有效执行约束。
